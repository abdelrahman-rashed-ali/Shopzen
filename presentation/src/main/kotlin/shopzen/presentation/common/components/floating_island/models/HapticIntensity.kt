package shopzen.presentation.common.components.floating_island.models

/**
 * Haptic intensity level. Maps to platform vibration amplitudes internally.
 */
enum class HapticIntensity {
    VERY_LIGHT,
    LIGHT,
    NORMAL,
    STRONG,
}
