package shopzen.domain.common

/** Shared domain constants used by pure business rules. */
object Constants {
    /** Maximum order total eligible for cash on delivery. */
    const val MAX_COD_AMOUNT: Double = 1_000.0
}
